# ============================================
# 后台管理系统 - 环境变量配置
# ============================================
# 使用说明：
# 1. 复制此文件为 .env.local
# 2. 修改其中的值为实际配置
# 3. .env.local 文件不会被Git提交，确保安全
# ============================================

# ============================================
# 数据库配置（PostgreSQL）
# ============================================
# 数据库主机地址
DB_HOST=localhost

# 数据库端口（默认5432）
DB_PORT=5432

# 数据库名称
DB_NAME=health_app

# 数据库用户名
DB_USER=postgres

# 数据库密码
DB_PASSWORD=your_database_password_here

# ============================================
# JWT配置（JSON Web Token）
# ============================================
# JWT秘钥（生产环境请修改为强密码，至少32字符）
# 生成方式：node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET=your-super-secret-jwt-key-change-in-production-at-least-32-characters

# JWT过期时间（默认7天）
# 格式：数字 + 单位（s=秒, m=分, h=小时, d=天）
# 示例：7d = 7天, 24h = 24小时, 30m = 30分钟
JWT_EXPIRES_IN=7d

# ============================================
# 应用配置
# ============================================
# 端口（Next.js开发服务器，默认3000）
PORT=3000

# 运行环境（development | production）
NODE_ENV=development

# ============================================
# 其他配置（可选）
# ============================================
# API基础URL（生产环境可以设置）
NEXT_PUBLIC_API_URL=http://localhost:3000

# ============================================
# 安全建议
# ============================================
# 1. 生产环境必须修改JWT_SECRET为强密码
# 2. 数据库密码使用强密码
# 3. .env.local 文件不要提交到Git
# 4. 生产环境使用环境变量管理工具（如Vercel环境变量）
# 5. 定期更换JWT_SECRET（会导致所有用户需要重新登录）

# ============================================
# 快速开始
# ============================================
# 1. 复制配置文件：cp env.local.example .env.local
# 2. 修改数据库密码和JWT_SECRET
# 3. 启动项目：npm run dev
